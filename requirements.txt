# PaddlePaddle inference engine is installed separately because the GPU wheel
# must match the user's CUDA and driver environment.
paddleocr[doc-parser]==3.4.0
PyMuPDF==1.27.2.2
# OpenCV is supplied as opencv-contrib-python by PaddleOCR/PaddleX.
# Do not install opencv-python alongside it; both packages provide cv2.
Pillow==12.1.1
numpy==1.24.4
tqdm==4.67.3
