# 工作日志

这里记录公开版本中比较重要、使用者能够直接感受到的变化。

## v281（2026-09-16）

本次公开版本汇总内部脚本 274～281，并首次提供可以直接运行的 Windows 发布包。

### 首次提供可直接运行的 Windows 包

- 发布 Windows x64 ZIP 和自解压 EXE，普通用户无需安装 Python。
- 安装包内置程序、四个基础 OCR 模型和使用说明，完整解压后即可启动。
- 同时发布 v281 单文件统一源码和 SHA256 校验文件。
- ZIP 与自解压 EXE 内容相同，用户任选一种下载即可。

### 更安全透明地准备 NVIDIA 运算库

- 不再把约 2.2 GB 的 CUDA 运算库塞进主程序包，降低初次下载体积。
- 只有在用户通过图形界面明确确认或执行 `--install-cuda` 后才会联网下载。
- 固定下载组件的版本、文件大小、官方地址和 SHA256，下载后自动校验。
- 在隔离目录中检查并解压，避免压缩包中的异常路径写到目标目录以外。
- 默认依次尝试清华大学镜像、北京外国语大学镜像和 PyPI，也可用 `--cuda-source` 指定来源。
- 增加 CUDA 下载日志，失败时可以直接查看具体阶段和原因。
- 如果电脑上已经存在可用组件，程序会优先检测并复用，避免重复下载。

### 竖排识别放到主页面

- 把“竖排文本识别”从高级选项移到主页面，位于页码范围和自动打开选项之间。
- 更方便用于**印刷体古籍及日语竖排文本识别**。
- 竖排模式仍需手动开启，不会自动改变普通横排文档的处理方式。

### 修正特殊 PDF 的文字层位置

- 加入 PDF CropBox 纵向补偿，修正部分裁切页面中隐藏文字层整体上下偏移的问题。
- 保留四点框倾斜校正、中文标点宽度调整和竖排文字层定位。
- 改动只作用于文字层坐标计算，不改变原始扫描图像。

### 改善界面与打包体验

- 更新应用名称、图标、顶部状态和错误提示，使打包版的运行状态更容易理解。
- 改善“开始处理”按钮在不同状态下的文字可读性。
- 移除较慢且体积更大的 PP-StructureV3 回退，统一使用轻量版面边界分析流程。
- 修正首次弹出 CUDA 说明时窗口显示不清楚的问题，并让下载窗口风格与主界面保持一致。
- 明确提示每次启动都需要初始化 AI 引擎，避免用户误以为程序没有响应。

### 验证与已知限制

- 已检查统一源码语法、命令行帮助、CropBox 对齐测试和发布安全测试。
- 已用 7-Zip 完整测试 ZIP 与自解压 EXE，并记录发布文件 SHA256。
- 发布包不包含完整 CUDA Toolkit；首次确认下载运算库时需要网络和足够磁盘空间。
- 当前 EXE 未进行代码签名，Windows SmartScreen 可能显示提醒。
- 低分辨率密排小字、超大字号扉页、连点目录和复杂横竖混排仍可能漏识别或错位。

## v32（2026-08-22）

本次公开版本对应内部脚本 263～273。

### 改善倾斜文字层对齐

- 默认读取 OCR 的真实四点框，让隐藏文字层跟随扫描页中文字行的倾斜角度。
- 短文字框可以借用整页倾角中位数，减少角度不稳定。
- 加入 0.3 度死区，标准水平页面继续按原有方式写入。
- 新增 `--no-skew`，可以关闭倾斜校正；`--skew-max` 用于调整异常角度上限。
- 作者测试中，4 度倾斜样张的文字层垂直错位中位数由 13.6 pt 降至 1.2 pt。

### 新增竖排文字模式

- 新增 `-V` / `--vertical`，支持自上而下、自右向左的中日韩竖排文字层。
- 竖排列序会自动改为从右向左，改善复制后的正文顺序。
- 竖排字号同时参考列长、字数和列宽，减少文字层压到相邻列。
- 对跨列误并的文字框进行拆分，让各列首字回到对应列中。
- 竖排模式会自动关闭文字行方向分类，并跳过只适合横排页面的版面边界分析。
- 该功能必须手动开启；程序不会自动把普通横排文档改成竖排。

### 加强低分辨率文档检测

- 命令行新增 `--dpi` 和 `--max-pixels`，可直接控制主识别渲染清晰度。
- 图形界面新增“300 DPI 强化检测”档，提高检测输入上限，适合低分辨率密排小字。
- 新增 `--multi-scale`，可使用 3、5 或 7 个邻近尺度重复检测并合并结果。
- 新增 `--det-thresh` 和 `--det-box-thresh`，可降低检测阈值以保留淡笔画和弱文字框。
- 竖排低分辨率扫描件会自动提高渲染与检测上限；用户显式指定参数时以用户设置为准。
- 强化检测会明显增加显存和处理时间，不建议对普通清晰文档默认开启。

### 改进启动和打包兼容性

- 自动探测 NVIDIA GPU；没有可用 GPU 时回退 CPU，并明确提醒速度会很慢。
- 增加 Windows 中文路径防护，降低模型因安装路径含中文而无法读取的风险。
- 打包版可把模型缓存放在程序目录旁，并在必要时迁移到纯英文路径。
- 轻量打包版加入首次获取 CUDA 运算库的准备逻辑；源码运行时不会重复下载这些库。
- 新增 `--selftest`，检查模型缓存、依赖、Paddle 设备和三个推理引擎。
- 命令行处理失败时返回失败退出码，便于批处理脚本判断结果。

### 改善操作界面

- 高级选项块统一顶部对齐，说明文字控制在两行以内。
- 为高级选项增加悬停说明，界面保持简洁的同时保留完整解释。
- 修正顶部控件被长提示文字压缩的问题，“完成时自动打开”等控件可以完整显示。
- 清晰度档位直接写出 DPI 和预计显存需求，降低选择难度。
- 新增竖排文本、关闭倾斜校正和多尺度深度扫描开关。

### 明确仍存在的识别限制

- 低分辨率密排扫描件中的大字正文效果较好，但双行小注仍可能大量漏识别。
- 作者以原文序列比对后测得双行小注召回率约为 32%，暂时不能视为可完整识别。
- 超大字号竖排扉页、连点目录、复杂横竖混排和自身带页面旋转的 PDF 仍可能异常。
- 多尺度与降低阈值只能改善部分检测问题，不能恢复源图中已经模糊或粘连的笔画。
- 当前仓库仍只发布源代码，不包含模型、CUDA、PaddlePaddle GPU 安装包或 EXE。

## v31（2026-08-21）

### 提高处理速度

- 默认改用更轻量的版面检测方式，只分析正文边界，减少重复 OCR。
- 默认把文字检测图像的最大边长限制为 1600 像素，减少超大页面的处理时间。
- 默认使用更快的文字层写入方式，不再逐字符重复处理。
- 资源回收由“每页一次”改为“每 10 页一次”，减少频繁停顿。
- 如需对照旧行为，可使用 `--layout-engine v3`、`--det-limit 0` 或 `--legacy-text`。

### 降低显存和内存占用

- 版面边界分析完成后立即释放版面模型，把更多显存留给正文 OCR。
- 给渲染图像增加 4000 像素的安全上限，避免异常大页面突然占满显存。
- 双联页拆分结果默认使用 JPEG 质量 88 暂存，降低内存和临时文件体积。
- 定期清理 MuPDF、Python 和 GPU 缓存，并在任务结束后删除拆分临时文件。
- 如需保留无损拆分页，可使用 `--split-lossless`，但会占用更多内存和磁盘空间。

### 新增双联页拆分

- 新增 `-D` / `--split-double-page`，可把一张跨页扫描拆成左右两个独立页面。
- 默认自动寻找靠近页面中心的装订中缝；识别不准时可以改用固定比例。
- 支持调整中缝搜索范围、切分比例、中缝留白和拆分 DPI。
- 支持用 `--split-skip` 跳过封面、折页等不应拆分的物理页。
- 支持用 `--split-rtl` 先输出右半页，适合从右向左翻阅的书籍。
- 启用后，普通跨页通常会变成两个输出页面；页码参数按拆分前的物理页计算。

### 增加诊断工具

- 新增 `--timing`，报告拆分、版面分析、文字识别和文字写入等阶段耗时。
- 启动时增加设备状态检查，并在可用时显示 GPU 和显存信息。
- 增加显存异常提示，便于发现 Paddle 实际没有使用预期 GPU 的情况。
- 新增 `--det-limit` 和 `--gc-interval`，可以按电脑配置调整速度与资源占用。

### 改善操作界面

- 高级选项区域改为可滚动，窗口较小时也能看到全部选项。
- 增加双联页拆分和分段计时开关。
- 长说明文字会自动换行，减少文字被截断。
- 选项按常用程度重新排列，默认参数会随 150、220、300 DPI 档位调整。

### 修正使用细节

- 命令行页码输入错误时会明确报告失败，不再显示错误的成功提示。
- 改进拆分临时文件的关闭与清理，减少文件被占用的情况。
- 保留 v25-1 的稳定横排文字逻辑，没有重新启用不可靠的竖排写入实验。
- **v31 当时尚未提供竖排写入；`--split-rtl` 只调整左右半页顺序。竖排模式从 v32 起通过 `-V` 手动开启。**

## v25-1（上一公开稳定版）

### 稳定横排文字层

- 调整常见中文全角标点的文字层占位宽度，改善横排文字与扫描图的贴合。
- 回滚会影响正常横排识别的竖排文字实验。
- 明确把竖排文字错位列为已知限制。

### 完善首次公开发布

- 整理图形界面和命令行的使用说明。
- 补充安装依赖、GPU 环境、隐私、许可证和常见问题文档。
- 公开源代码，不包含模型、CUDA、PaddlePaddle GPU 安装包或 EXE。
