# 工作日志

这里记录公开版本中比较重要、使用者能够直接感受到的变化。

## v31（2026-08-21）

### 提高处理速度

- 默认改用更轻量的版面检测方式，只分析正文边界，减少重复 OCR。
- 默认把文字检测图像的最大边长限制为 1600 像素，减少超大页面的处理时间。
- 默认使用更快的文字层写入方式，不再逐字符重复处理。
- 资源回收由“每页一次”改为“每 10 页一次”，减少频繁停顿。
- 如需对照旧行为，可使用 `--layout-engine v3`、`--det-limit 0` 或 `--legacy-text`。

### 降低显存和内存占用

- 版面边界分析完成后立即释放版面模型，把更多显存留给正文 OCR。
- 给渲染图像增加 4000 像素的安全上限，避免异常大页面突然占满显存。
- 双联页拆分结果默认使用 JPEG 质量 88 暂存，降低内存和临时文件体积。
- 定期清理 MuPDF、Python 和 GPU 缓存，并在任务结束后删除拆分临时文件。
- 如需保留无损拆分页，可使用 `--split-lossless`，但会占用更多内存和磁盘空间。

### 新增双联页拆分

- 新增 `-D` / `--split-double-page`，可把一张跨页扫描拆成左右两个独立页面。
- 默认自动寻找靠近页面中心的装订中缝；识别不准时可以改用固定比例。
- 支持调整中缝搜索范围、切分比例、中缝留白和拆分 DPI。
- 支持用 `--split-skip` 跳过封面、折页等不应拆分的物理页。
- 支持用 `--split-rtl` 先输出右半页，适合从右向左翻阅的书籍。
- 启用后，普通跨页通常会变成两个输出页面；页码参数按拆分前的物理页计算。

### 增加诊断工具

- 新增 `--timing`，报告拆分、版面分析、文字识别和文字写入等阶段耗时。
- 启动时增加设备状态检查，并在可用时显示 GPU 和显存信息。
- 增加显存异常提示，便于发现 Paddle 实际没有使用预期 GPU 的情况。
- 新增 `--det-limit` 和 `--gc-interval`，可以按电脑配置调整速度与资源占用。

### 改善操作界面

- 高级选项区域改为可滚动，窗口较小时也能看到全部选项。
- 增加双联页拆分和分段计时开关。
- 长说明文字会自动换行，减少文字被截断。
- 选项按常用程度重新排列，默认参数会随 150、220、300 DPI 档位调整。

### 修正使用细节

- 命令行页码输入错误时会明确报告失败，不再显示错误的成功提示。
- 改进拆分临时文件的关闭与清理，减少文件被占用的情况。
- 保留 v25-1 的稳定横排文字逻辑，没有重新启用不可靠的竖排写入实验。
- **竖排文字写入仍未解决；`--split-rtl` 只调整左右半页顺序，不代表支持竖排 OCR。**

## v25-1（上一公开稳定版）

### 稳定横排文字层

- 调整常见中文全角标点的文字层占位宽度，改善横排文字与扫描图的贴合。
- 回滚会影响正常横排识别的竖排文字实验。
- 明确把竖排文字错位列为已知限制。

### 完善首次公开发布

- 整理图形界面和命令行的使用说明。
- 补充安装依赖、GPU 环境、隐私、许可证和常见问题文档。
- 公开源代码，不包含模型、CUDA、PaddlePaddle GPU 安装包或 EXE。
